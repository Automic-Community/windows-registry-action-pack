*** Settings ***
Suite Setup       Suite Setup
Force Tags        import key    invalid    win
Test Template     Import Key Template
Resource          ../../resources/keywords.txt

*** Variables ***

*** Test Cases ***    File Name
Import Key Invalid File
                      ${_WORKING_DIR}\\import\\invalid\\COMPONENTS

Import Key Invalid Nonexisting Folder
                      ${_WORKING_DIR}\\invalid

Import Key Invalid Readonly Folder
                      ${_WORKING_DIR}\\readonly

*** Keywords ***
Suite Setup
    Init AE And Package
    Init Registry Server

Import Key Template
    [Arguments]    ${file}
    Init Action    ${ACTION_IMPORT_KEY}
    Action Set    ${PRT_REG_FILE}    ${file}
    Action Execute
    Action Report Should Be Found
    Action Return Code Should Be    1
    Action Return Status Should Be    ${ENDED_NOT_OK - aborted}
    Action Report Should Contain    ${ErrorImportingKey}
    Action Report Should Contain    START AT
    #Action Report Should Contain    PARAMETER VALIDATION START
    #Action Report Should Contain    EXECUTION START
    #Action Report Should Contain    ABEND AT
    #Action Report Should Contain    RET-CODE
