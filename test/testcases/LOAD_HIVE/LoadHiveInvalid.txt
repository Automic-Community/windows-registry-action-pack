*** Settings ***
Suite Setup       Suite Setup
Force Tags        load hive    invalid
Test Template     Load Hive Template
Resource          ../../resources/keywords.txt

*** Variables ***

*** Test Cases ***    Key Name (KEY)
Load Hive Invalid To Wrong Key Root
                      HKCC\\COMPONENTS

Load Hive Invalid To Key Root Not Exist
                      HKCC1\\COMPONENTS

*** Keywords ***
Suite Setup
    Init AE And Package
    Init Registry Server

Load Hive Template
    [Arguments]    ${keyName}
    Init Action    ${ACTION_UNLOAD_HIVE}
    Action Set    ${PRT_REG_KEY}    ${keyName}
    Action Execute
    Init Action    ${ACTION_UNLOAD_HIVE}
    Action Set    ${PRT_REG_KEY}    ${keyName}
    Action Execute
    Init Action    ${ACTION_DELETE}
    Action Set    ${PRT_DELETE_DIR_FILE_RM_NAME}    ${_WORKING_DIR}\\hive\\invalid
    Action Set    ${PRT_DELETE_DIR_FILE_RM_FAIL}    NO
    Action Execute
    Init Action    ${ACTION_LOAD_HIVE}
    Action Set    ${PRT_REG_KEY}    ${keyName}
    Action Set    ${PRT_REG_FILE}    ${_WORKING_DIR}\\hive\\invalid
    Action Execute
    Action Report Should Be Found
    Action Return Code Should Be    1
    Action Return Status Should Be    ${ENDED_NOT_OK - aborted}
    Action Report Should Contain    ${ErrorLoadingHive}
    Action Report Should Contain    START AT
    #Action Report Should Contain    PARAMETER VALIDATION START
    #Action Report Should Contain    EXECUTION START
    #Action Report Should Contain    ABEND AT
    #Action Report Should Contain    RET-CODE
