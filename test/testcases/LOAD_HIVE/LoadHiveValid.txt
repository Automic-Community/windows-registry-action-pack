*** Settings ***
Suite Setup       Suite Setup
Force Tags        load hive    valid
Test Template     Load Hive Template
Resource          ../../resources/keywords.txt

*** Variables ***

*** Test Cases ***    Key Name (KEY)
Load Hive Valid To HKU
                      HKU\\TESTLOAD

*** Keywords ***
Suite Setup
    Init AE And Package
    Init Registry Server

Load Hive Template
    [Arguments]    ${keyName}
    Init Action    ${ACTION_UNLOAD_HIVE}
    Action Set    ${PRT_REG_KEY}    HKLM\\TESTLOAD
    Action Execute
    Init Action    ${ACTION_UNLOAD_HIVE}
    Action Set    ${PRT_REG_KEY}    HKU\\TESTLOAD
    Action Execute
    Init Action    ${ACTION_LOAD_HIVE}
    Action Set    ${PRT_REG_KEY}    ${keyName}
    Action Set    ${PRT_REG_FILE}    ${_WORKING_DIR}\\hive\\load\\TESTLOAD
    Action Execute
    Action Report Should Be Found
    Action Return Code Should Be    0
    Action Return Status Should Be    ${ENDED_OK - ended normally}
    Action Report Should Contain    ${SuccessfullyLoadedHive}
    Action Report Should Contain    START AT
    #Action Report Should Contain    PARAMETER VALIDATION START
    #Action Report Should Contain    EXECUTION START
    #Action Report Should Contain    ABEND AT
    #Action Report Should Contain    RET-CODE
